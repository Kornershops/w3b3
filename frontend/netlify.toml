[build]
command = "cd frontend && npm install --legacy-peer-deps --force && npm run build"
publish = "frontend/.next"

[build.environment]
SECRETS_SCAN_ENABLED = "false"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
